# Java-Secure-Multi-Client-Chat
Secure multi-client TCP chat application in Java with SHA-256 hashing and automatic conversation backup system.
# Secure TCP Multi-Client Chat Application

A Java-based secure client-server chat application that supports multiple clients using TCP connections.  
The project implements SHA-256 hashing to ensure message integrity and includes an automatic conversation backup system.

---

## 🔐 Features

- Multi-client TCP chat server
- Secure message integrity using SHA-256 hashing
- Automatic conversation backup with timestamps
- Client-side and server-side logging
- Simple and user-friendly interface
- Cross-platform (Windows / Linux / Mac)

---

## 🛠️ Technologies Used

- Java
- TCP Sockets
- SHA-256 (Java Security)
- File I/O
- NetBeans IDE

---

## 📂 Project Structure

Server/ → Server-side code
Client/ → Client-side code
backups/ → Conversation log files


---

## 🚀 How to Run the Project

1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/Secure-TCP-Chat-Application.git
Run the server:

Open Server.java

Start the server (Port 5000)

Run the client:

Open Client.java

Connect to localhost
